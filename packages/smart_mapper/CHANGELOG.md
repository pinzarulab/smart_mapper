## 0.2.0

- Added strict analyzer-based type validation.
- Added `MapRelation`, `MapIgnore`, `MapDefault`, and `MapConstructor`.
- Added custom converters through `MapField.using`.
- Added `Set`, `Iterable`, nullable-item, nullable-collection, and ObjectBox
  `ToOne` mapping support.

## 0.1.0

- Initial Clean Architecture mapper annotations.
- Added explicit per-method field renaming.

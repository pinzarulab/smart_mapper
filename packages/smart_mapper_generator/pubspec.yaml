name: smart_mapper_generator
description: Code generator for the smart_mapper Clean Architecture mapper.
version: 0.1.0

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  analyzer: '>=8.1.1 <15.0.0'
  build: ^4.0.0
  source_gen: ^4.2.4
  smart_mapper: ^0.1.0

dev_dependencies:
  build_runner: ^2.16.0
  lints: ^6.0.0
  test: ^1.25.0
